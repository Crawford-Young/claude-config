---
name: orchestrate
description: SUPERSEDED by the agent-factory skill (2026-07-15). Kept only so stale references still route. For any multi-task plan execution, spawn/dispatch decision, model routing, or performance logging, use the agent-factory skill.
---

# Orchestrate (superseded)

Superseded by the `agent-factory` skill (2026-07-15). The tier ladder (T1–T4), plan profiles (Precision/Standard/Velocity), and the Layer-2 routing table are retired.

- Spawn protocol, dispatch template, escalation, performance MDs → `../agent-factory/SKILL.md`
- Routing authority → `claude-config/agents/profiles/<type>.md` (type definitions are general guidance only)

Read `../agent-factory/SKILL.md`.
