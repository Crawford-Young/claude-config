# Fixture archive (history — nothing loads this)

A relocated rule that must be reachable, not merely present somewhere on disk, because a
gate that only proves presence cannot tell an archive from a live file.
