# reviewer — profile

## Strengths
- Spec-compliance + quality review of a bounded diff (seed — routing-table default)
- Byte-compare of verbatim-code tasks — needs no strong model (seed)
- **Given the implementer's own deviation list to adjudicate (not rediscover) → one-pass mechanism-level verdicts** — w3L: 5/5 reviews grade 5, 0 Critical/Major whole wave, 0 false Criticals; adjudications cited runtime mechanisms (Radix sentinel, noValidate contract) not preference. Provisional: one wave (2026-07-16 w3L)
- Pairing "independently rerun X" with the SPECIFIC evidence gap in the dispatch produces exactly the missing proof (w3L L4 coverage rerun; n=1)
- **sonnet combined single review scales to 22-file verbatim batches** — eb3: one pass covered byte-compare + quality + cross-cutting greps (old-copy strings, non-async-export, shim imports) + independent gate rerun; zero findings, zero false alarms. Sonnet-for-byte-compare seed now n=2 (w1 lib + eb3). (2026-07-16 eb3)
- **sonnet combined review for verbatim rollout tasks — FIRM (n=5+ across eb3/eb4):** eb4 added 3 more grade-5 combined reviews (w4a lib, w4b, w4c), incl. sound one-pass adjudication of an implementer deviation (w4c CSS mock: verified postcss string-plugin form, zero prior CSS-importing tests, mock inert for assertions, probe-revert residue-free). Zero false alarms across both waves. (2026-07-16/17 eb4)
- **Adjudication-style briefs (enumerate probes + implementer's flagged decisions) — FIRM, n=10+ (cybond w3, 2026-07-16→21):** ten consecutive grade-5 sonnet reviews, zero false Criticals whole wave; every probe answered with file:line or command evidence; enumerated probes did not crowd out discovery (unprompted findings at A4/A7/A8). Pre-stating the test-accounting basis (total vs green-only) converts forensic reconciliation into cheap verification (A6→A7 confirmed).
- **"Try to construct a bypass" phrasing in security probes → actual attack attempts with per-attempt disposition** — stronger than "verify the check works" (w3 A9-hardening: 3 bypasses constructed and held; A10: 2 genuine spec-edge findings routed to issue log). n=3 (2026-07-17/18)
- **Implementer environment-limitation claims are adjudicable against installed dist source** — "read the .mjs render path" refuted an opus mis-diagnosis evidenced with a DOM dump (w3 A11, 2026-07-19). Standardize the probe for any "test env can't do X" deviation. n=1
- **sonnet combined review widened to small behavioral patches with latitude — FIRM (n=4: w3 A12/A13 + motion-pass T8a lib patch + T6):** all zero-real-finding, mechanism-level adjudications. T8a additionally verified fail-first by running new tests against the PARENT commit (`git show <sha>~1:`) unprompted, and correctly self-diagnosed its scoped-coverage EXIT:1 as a global-threshold artifact instead of reporting a false FAIL. (2026-07-20/21)
- **opus whole-branch final review with a verbatim global-constraints block → constraint-by-constraint verification with file:line cites, zero false flags** — slot-W1: adjudicated `??` node-slots vs the `!== undefined` render-fn rule correctly instead of false-flagging, checked axe story-ID resolution + README row coverage unprompted, correctly separated pre-existing style debt from wave-introduced findings. Provisional n=1 (2026-07-21)
- **`git diff -w` disambiguates prettier-reflow from content drift on large verbatim diffs** — motion-pass T7: a 260-line reindented page verified as zero-content-drift in one command instead of a false drift flag. Cheap, decisive; standardize in byte-compare briefs. n=1 (2026-07-21)
- **sonnet combined review extends to GDScript/Godot-scene domain — FIRM (phases 1/2/3a):** phase 1: 8 reviews all grade 5, zero false alarms; independent verification genuinely mathematical (recomputed all 8 marker spans from raw tscn transforms, hand-recomputed 5 test expectations, traced synchronous-emit frozen-guard double-fire proof, capsule-height clearance math from player.tscn). Phase-3a added 4 more grade-5s (verbatim TDD + integration-grade player rebuild), zero redos whole wave; T3 review's lab-parity probe ("diff vs frozen reference, flag any OTHER divergence") is cheap + decisive for adopted-core ports — reuse for any rebuild-on-reference task. Domain-agnostic: adjudication-style briefs carry the lane. (2026-07-21/25)
- **opus whole-branch final review — FIRM n=3 (slot-W1, what-is-dark p2, wid phase-3a):** feed it the ledgered-Minor list + global-constraints block verbatim → constraint-by-constraint verification with cites, zero false flags across all three. **Ledger-recount-refutes-ledger now n=2:** p2 found a ledgered Minor self-resolved (load_steps); phase-3a REFUTED the ledger's own "correct 8.64" as a double-rounding artifact (exact 8.6232, shipped 8.63 fine at stated precision) — the ledger had carried a wrong reviewer correction all wave. Phase-3a also: caught the wave's only real blocker (trailer missing on auto-generated `git revert` commits) WITH a correct remediation trap analysis (amending the probe commit dangles the revert's SHA reference → drop the net-zero pair), found a real docs bug (erosion "air-only" wording vs ground-accel-scaling code) that implementer + orchestrator + per-task reviews all shipped, and proved corner-crease unreachability structurally (62/62 collision shapes axis-aligned) instead of by inspection. (2026-07-22/25)

- **Doc-staleness catches on model-replacement tasks** — the file carrying a documented trap (AGENTS.md) is exactly the file the change invalidates; T13 review's only Major was the stale fab entry, which the implementer's clean report structurally couldn't see (outside its file scope). Probe candidate for any brief replacing a documented model: "is the AGENTS.md entry describing this feature still true?" Provisional n=1 (2026-07-22 slot-w1 app T13)
- **Environment-probe contamination adjudication** — QA-R1 review correctly separated diff-verifiable probes from a gate re-run contaminated by a concurrent warm implementer in the same worktree (adjudicated benign instead of false-failing). n=1 (2026-07-22)
- **"An axe suppression that survives a shell rework is a smell" — probe any rework diff carrying a rule disable:** gs-p1 QA-R1 sonnet combined review caught a `region` disable masking a genuine WCAG landmark gap that BOTH the implementer and orchestrator had accepted; fix (`<header>` wrap) removed both suppressions for genuine-zero axe both themes. When a suppression "moves" during a rework, ask whether the violation is real. n=1 (2026-07-22)

- **Adjudication-style count +2 (GS-P0 T3/T4, 2026-07-22):** both sonnet, both grade 5 on 6–7-file SDK-migration diffs; T3 independently dist-verified no-double-count in billing and corrected the dispatch's own wrong tsc-exit-code assumption; T4 resolved a pre-framed cross-task integration probe (empty-content wire risk) definitively with route+dist evidence and classified 2 Minors pre-existing via git show comparison.
- **Adjudication-style count +11 (event-series A1–A9, 2026-07-23/24) +5 (visibility-w1 V1/V3–V6, 2026-07-24/25):** eleven consecutive sonnet grade-5s across migration, novel calendar/DST algorithm, 'use server' auth surfaces, 8-file UI port, chat rewire, DB teardown; zero false Criticals (one false-positive Minor at A9, implementer-adjudicated with orchestrator verification). Node hand-verification of calendar math ran unprompted in 4 reviews. Visibility-w1: five more sonnet grade-5s, zero false Criticals; V1 caught a real Major in a file the implementer was scoped OUT of (Storybook demos silently broken — cross-file blast radius n=2 with event-series A6); V6 threat-model probe traced both mappers + WHERE construction + sole-caller grep and independently reproduced gates.
- **Probes that NAME an invariant get its consumers traced cross-module** — event-series A6: probe said "trace what the page fetches"; reviewer chased the INSERT path too and caught standalone-series occurrences leaking into the one-off query (real Major through 100%-green gates, in a file the task never touched — the invariant-consumer class per-task implementation structurally cannot see). Brief pattern: state the invariant verbatim ("activityId IS NULL = one-off") in the probe. n=1 (2026-07-24)
- **Live in-engine simulation on geometry/physics diffs catches what static review structurally cannot — FIRM (momentum-lab, 2026-07-23/24):** 3 real Critical/Major catches across the wave (kicker momentum reversal via 5 live physics sims; R11 lip strike at v46.5 via live hold-style ride after implementer's Y-Z projectile check was blind to 3D edge contacts; apron underside strike via the mandated lateral sweep the implementer's narrowed matrix hid). GDScript reviewer briefs on any geometry change MUST mandate live-sim with a full parameter matrix — sweep narrowings proposed by the implementer are re-review flags, not accepted defaults. ~12 sonnet reviews this wave, all grade 5, zero false Criticals.
- **Checkable report claims get checked** — wave-close review disproved an implementer's "byte-matches pre-probe state" claim with a three-way `git show` compare (real Major; residue survived grep-zero + full GUT + clean launch), and correctly downgraded Critical→Major on functional-harmlessness evidence. Brief pattern: name the claim and the verifying command. n=1 (2026-07-24 momentum-lab)
- **sonnet latitude-geometry full-recompute lane FIRM (what-is-dark phase-2: 10 reviews grade 4–5, 3 real Majors, zero false Criticals, 2026-07-22/23):** recomputes every span/gap/marker from raw tscn transforms, re-derives movement facts from source (ray reach, respawn y), recounts load_steps from both git blobs, reruns gates itself. Caught implementer self-check looseness twice (both conservative-direction), corrected the ORCHESTRATOR's own probe arithmetic twice — adjudication briefs may carry rough numbers if framed "verify from the actual tscn, not these". Warm fix + warm re-review pairing n=3 (cost curve 16→9 tools): cheapest full-confidence close for latitude implementers with miss history.

- **Named-invariant probe count +1 in GDScript (dog-eat-dog 9b.2, 2026-07-25):** opus combined review traced the "bedtime-derived" invariant through all 5 consumers clean, AND caught the wave's one Major pre-playtest (M-A unbounded pull → softlock class — the exact games-domain boundary-math class documented as "found only in playtest"); adjudicated all 10 implementer latitude decisions with independent arithmetic (recomputed typewriter-window slack). Also flagged a brief-internal contradiction (no-delete rule vs replacement contracts) as ISSUE — brief-authoring lesson: when a contract replaces semantics, enumerate the tests that die with it.
- **opus whole-branch review at the integration point closes per-batch diff-verify debt — n=1 GDScript (dog-eat-dog T9, 2026-07-24):** every finding (2 design-gate Majors correctly routed to user playtest rulings, not implementer FAIL; 8 minors) was invisible to per-batch diff-verify; exemplary verdict discipline separating Major-impact from FAIL when resolution is a user ruling.
- **sonnet adjudication-brief lane +1 in GDScript (dog-eat-dog 9c.2, 2026-07-25):** 11/11 enumerated probes answered with file:line or command evidence, zero findings, both latitude decisions ACCEPTed with mechanism-level reasoning; correctly scoped review inside a dirty tree when the brief named the exact file set + warned about the foreign diff.
- **Batched sibling review catches cross-task defects per-task review structurally cannot — n=2 firm (carsickyak W1, 2026-07-24):** one dispatch over sibling tasks (T5+T6 adapters; T11–T13 pages) kept per-task verdicts sharp AND caught T12's deviation from the T11/T13 convention plus a latent adapter-constant-vs-page-invariant conflict (issue #5, plan-authored, no single task owned it). Sonnet, zero coverage loss, zero false Criticals; adjudication lane count +3 same wave (incl. "icon exists in lucide but is the generic glyph, not the brand" — the trap a name-only export check misses).

- **Named-invariant probe count +1, opus (dog-eat-dog w2 T8, 2026-07-26):** combined integration review, 8/8 named probes traced with file:line, 3 real Majors zero false alarms — incl. an out-of-diff T7 bar-scale bug found BECAUSE a probe named the sick invariant, a cross-module balance-arithmetic kill (sick branch unreachable at default balance — upgraded the implementer's own telegraph flag to root cause), and a rendering defect (autowrap under grown copy) no gate sees. Named-invariant probes now n≥3 across domains; keep mandating probes by invariant name, not file list.
- **Adjudication-style count +3, incl. a correctly-delivered FAIL (google-oauth-hardening, 2026-07-26):** three sonnet combined reviews (T1–4 security 9-probe, T5–6, T7–8), all grade 5, zero false Criticals. T7–8 FAIL was real: reviewer ran ISOLATED coverage itself unprompted and caught a plan-authored test-branch-parity gap both implementer AND orchestrator shipped past — "reviewer independently reruns gates" now the proven catch mechanism for coverage classes (n≥3). T1–4 verified the merge invariant two independent ways (repo grep + installed @auth/core handle-login.js source read); T5–6 P2 open-redirect probe went to installed @auth/core init.js + node repro of the backslash trick. Enumerated security probes (bypass-construction style) keep producing per-attempt dispositions.

- **Adjudication-brief → sonnet holds outside application domains — n=1 (routing-verification T6, 2026-07-28):** PASS 0C/0M/3 Minor on a zero-dependency node script with no framework, no coverage gate, and no `just check` to rerun. The "reviewer independently reruns gates" mechanism still supplied the value, in the only form available: it built a **synthetic baseline** to prove `MISSING` and `UNREACHABLE` print together without masking — a case the author's own suite had not isolated — reproduced all three named constraints live, and cross-checked the exemption entry's `CLAUDE.md:27` citation against the real file. Its one carried Minor (config aborts and real failures sharing exit code 1) was fixed at reflect. Read as: enumerated probes are what make sonnet sufficient, not the presence of a familiar toolchain.

- **opus whole-branch final review — n=4, zero false Criticals across all four (what-is-dark 3b T9.4, 2026-07-28).** 28 commits, 10 enumerated probes, all complete; reproduced every gate itself before using any number from the dispatch. **The transferable probe is marker/invariant-under-FINAL-state:** it verified all 24 respawn markers × 5 offsets × 2 loops by live raycast — the geometry version of the invariant-consumer class, since markers were placed in one task and 58 erodable bodies + 5 chasms were opened underneath them by three LATER tasks, so no per-task review could see the pairing (a respawn over a chasm is a softlock that passes every gate). Its one Major was live-proved rather than inferred (drove the real `_physics_process` with zero jump input, 10/10 rode at `SINK_CAP`, disproving a README controls sentence). **Three highest-risk confirmations all held** — probe residue byte-clean with `load_steps` recounted from the file, chasm lips exact at plateau AND absent at loop 0 as a negative control, ruling premises re-derived true — which is the argument for the lane: a whole-branch review that mostly CONFIRMS is still worth its cost when the confirmations are the load-bearing ones. Spawned 2 sonnet recon children of its own (doc-claim sweep, raw-tscn extraction), both grade 5, ~$6.67 of the run's cost — correct routing, cheaper than opus doing the fan-out inline.
- **Fable-vs-Opus-5 open question — ANSWERED for the high-stakes review lane (2026-07-28).** The 2026-07-24 release note ("route Opus 5 first, fable narrows to exceptional-stakes") was unvalidated; this wave validated it under forcing conditions, since the fable weekly cap was at 100% and the lane was unavailable at all. Opus 5 took a 28-commit whole-branch close review and delivered zero false Criticals, the wave's only real Major, and five correctly-classified observational findings. No evidence the fable lane was missed. n=1 real evidence replacing an assumption — keep fable for exceptional-stakes specs only.

## Weaknesses
- **"Structurally dead per SDK types" dead-code claims can be wrong-in-practice under test mocks** — GS-P0 T3: reviewer Minor flagged an `if (usage)` guard dead per v7 types; 2 persist-path test mocks call the callback without usage, guard was load-bearing in test env. Orchestrator hand-fix broke 2 tests, reverted. Rule: any dead-code claim gets a mock-callsite grep before acting. n=1 (2026-07-22)
- **Def has no Write tool — FIRM (n=3: slot-W1 2026-07-21, what-is-dark final review 2026-07-22, what-is-dark 3b 2026-07-28)** — briefs asking for a report FILE get an inline report instead; request "return full report inline; orchestrator persists" in every reviewer dispatch. **New failure mode at n=3: the agent went IDLE without delivering at all** — the dispatch said "full report INLINE in your final message (you have no Write tool)" and it still finished silently, work complete but unreported. Recovered at zero loss with one report-only SendMessage, which returned the entire report intact. Fix for the contract: state "your final message IS the deliverable — the orchestrator sees nothing else" rather than describing where the report should go. Cheap failure, but it costs a round-trip and looks like a dead agent
- Nonzero false-Critical rate — orchestrator verifies every Critical against source before dispatching a fix (2026-06-10; a wrong fix cycle costs a full dispatch + re-review)
- **Prescribed REMEDIES are not findings-grade by default** — after one exact remedy (T5b, fix went trivial-lane), the next round's remedy was geometrically unsound while claiming "verified, no recheck needed" (T5c: in-gap strip would have destroyed the near-limit jump it protected). Orchestrator verifies remedies from source before any fix path — n=1 unsound / n=1 clean (what-is-dark phase-2, 2026-07-22)
- Same trap-blindness as implementer when brief omits AGENTS.md citations (2026-07-14, w2.6 S14)

## Model sweet spot
- Default **opus** — but **sonnet matches opus when the brief is adjudication-style** (enumerated probes + flagged decisions): cybond w3 ran sonnet on ALL 10 app-wave reviews incl. security-load-bearing and integration diffs, ten grade-5s, zero false alarms (2026-07-21). Route sonnet by default when the orchestrator writes enumerated probes; keep opus for reviews where the orchestrator cannot pre-frame the holes.
- **sonnet** for mechanical spec-verbatim byte-compare (seed)
- **fable** for high-stakes design review (auth/payments/migration specs) — per-run user clearance required (seed fable lane)
- Route Opus 5 first for high-stakes design review — half fable cost, no clearance overhead; fable narrows to exceptional-stakes specs or after an Opus-5 review misses (release-note 2026-07-24, unvalidated — confirm at first reflect)
- Severity-filter recall depression is unchanged on Opus 5 (same as 4.7/4.8) — the existing report-everything, filter-downstream adjudication-brief pattern stays correct as-is (release-note 2026-07-24, unvalidated — confirm at first reflect)

## Spawn-worthiness
- Earns its cost after every implementer task with latitude; combined single-pass review suffices for verbatim tasks (2026-06-10)

## Open questions
- Fable design-review lane value — no scorecard has yet proven it earns its cost over opus
- With fable narrowed to exceptional-stakes-only, does Opus 5 close the gap on the remaining high-stakes (non-exceptional) design reviews fable used to take? First-wave evidence should confirm. (release-note 2026-07-24, unvalidated — confirm at first reflect)
