# docs-agent — profile

## Strengths
- Pure prose/MD work: CLAUDE.md edits, specs, ADRs, companion references (seed — routing-table default)
- **sonnet grade-5 on AGENTS.md/README wave-convention entries when briefed "read the shipped code, not memory" + explicit source-file list — FIRM n=3 (motion-pass T8 2026-07-21; event-series A10 2026-07-24; visibility-w1 V7 2026-07-25)** — motion-pass: 6 dense trap-anchored AGENTS.md entries + README additions, all code-sourced, caught mechanism details beyond the brief. Event-series: full activity-model→series-model rewrite; caught 3 factual errors IN THE BRIEF/EXISTING DOCS (schema richer than brief claimed; wrong migration numbers, journal-verified) and DISCOVERED a real shipped bug (dead edit-mode Repeat control, wave issue #7) by reading shipped code. Visibility-w1: caught 2 brief gaps (wrong action filename; create-vs-edit seeding split), fixed 2 stale bullets beyond the briefed one, migration number journal-verified, open question routed to report not doc. Doc rewrites double as an end-of-wave verification net — the agent reads what shipped, not what was planned. Parallel-safe with reviews (disjoint files); earns its spawn when the orchestrator's context is loaded/post-compaction. **Standing brief boilerplate (all 3 weakness mitigations held at n=1–2 each): cite the trap triple — "uncertainty goes in your report, never in the doc" + "verify re-stated paths still exist" + "describe commands, don't quote recipe templates".
- **Flags ambiguity instead of silently guessing** — restructure w1 T3: surfaced 5 routing ambiguities in a CLAUDE.md split, 2 of them real plan defects; w1 T4: refused to invent Godot/GUT CLI flags for a zero-coverage domain, wrote at held confidence and flagged the gap. Both sonnet grade-5, 0 redos. n=2 same wave — treat as one wave's evidence (2026-07-21). **n=3 (second wave): what-is-dark T9 — caught a brief error (claimed `just import` might cover the class-cache pass; proved flag combinations differ, documented manual step instead of inventing a recipe) + removed a stale vault control line by source-check (2026-07-22)**
- **sonnet grade-5 on repo README + CLAUDE.md creation when briefed "verify every claim against source"** — what-is-dark T9: every keybind, recipe name, and constant name source-verified (final opus review found 100% accuracy); parallel-safe against a user playtest gate (disjoint from code). n=1 (2026-07-22)

## Weaknesses
- No Bash — anything needing verification commands, git, or file moves is out of scope (seed — tools boundary)
- Quotes task-runner recipe templates verbatim into prose (`{{godot}}` Justfile var leaked into a CLAUDE.md gotcha line) — briefs citing Justfile/Makefile recipes should say "describe the command, don't quote recipe templates". Provisional: n=1 (2026-07-22 what-is-dark T9, orchestrator trivial-fixed)
- Leaks its own hedge prose INTO the published artifact ("verify exact range before citing" landed in AGENTS.md) — briefs must carry "uncertainty goes in your report, never in the doc". Provisional: n=1 (2026-07-24 event-series A10, orchestrator trivial-fixed)
- **Fixes an incidentally-found stale fact in ONE file and leaves its twin standing** — friends-w1 T8 (sonnet, grade 4): caught an unbriefed stale segment count in `AGENTS.md` and corrected it, then missed the identical line in `README.md`, which was in the same brief's file list. The find itself is the strength (it also self-caught two of its own drafting errors pre-finalize and verified every briefed fact against shipped source); the gap is treating an incidental find as a one-file fix rather than a repo-wide grep prompt. Now a standing line in the agent def. Provisional: n=1 (2026-07-27, orchestrator trivial-fixed)
- Carries stale paths/facts forward from source docs it was told not to rewrite — briefs touching existing constraint bullets must include "verify file paths you re-state still exist" (same class as implementer premise-verification lines). Provisional: n=1 (2026-07-18 marketing-reposition, grade 4: stale `src/lib/github.ts` path re-stated; 2 orchestrator trivial-fixes post-hoc). **Mitigation confirmed n=1:** a trap citation in the brief (naming this weakness + the paths at risk) produced zero stale paths on a path-heavy CLAUDE.md split (2026-07-21 restructure w1 T3) — keep citing in path-touching briefs

## Model sweet spot
- Default **haiku**; sonnet grade-4 on repo README+CLAUDE.md restructure — structure/tone faithful, verbatim-preserve honored (n=1, 2026-07-18)
- Effort: frontmatter-pinned "low" (G31, VERIFIED 2026-08-08 re-probe) — sonnet-model dispatch of recon def emitted OTel `effort:"low"` (concurrent session-default sonnet showed `"high"`); haiku rows omit the attr because haiku has no effort dimension, not because frontmatter is ignored; pin survives dispatch-time model override (n=1)

## Spawn-worthiness
- Marginal — orchestrators with content already in context historically ran doc waves inline faster (2026-07-01, native-agents wave: 11 tasks inline, 0 redos). Spawn only when doc volume would blow the spawner's context.

## Open questions
- Haiku prose quality on nuanced standards docs — thin data; watch grades
