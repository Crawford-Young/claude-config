# implementer — profile

## Strengths
- Bounded single-task TDD implementation with exact file scope (seed — routing-table default, evidence across waves since 2026-06)
- Warm redos on fixable same-model failures — SendMessage beats cold re-dispatch (seed — redo protocol history)

## Strengths (cont.)
- **sonnet grade-5 on wide-but-mechanical breaking surgery when the brief names the freshest sibling to mirror** — w3L L2 (2-file threading copy, warm redo clean) + L5 (deletion+rebuild mirroring L4 conventions, 87 tool uses, 4 sound self-flagged deviations, diagnosed parallel-axe flake correctly instead of thrashing). Provisional: n=2 dispatches, one wave (2026-07-16 w3L)
- opus recovers deleted patterns from git history unprompted when rebuilding over a stripped API (w3L L4: L1-deleted Days-toggle recovered from `9dabefe^`; n=1)
- **sonnet grade-5 on combined many-file verbatim batches** — eb3 Tasks 2–4: ONE dispatch, 22 files (11 boundaries + 11 tests, near-identical canonical pattern), TDD per file honored, all gates first-pass, zero findings at review; surfaced the repo's coverage-exclude config unprompted. Batch-combining near-identical verbatim tasks beats per-task cold starts. Provisional: n=1 (2026-07-16 eb3)
- **sonnet grade-5 on cross-repo verbatim rollout tasks — FIRM (n=4, eb4 w4a–w4d, 2026-07-16/17):** four dispatches across three repos, zero redos, zero escalations. Handles repo-specific traps from brief citations (port recon, gated-consume dist checks both directions, background-gate EXIT lines). Self-flagged deviations consistently sound (scoped CSS mock w4c, probe-reverted).
- **sonnet grade-5 on bundled QA rounds — ONE dispatch with per-fix prescriptions** (4 user-reported fixes / 7 files incl. a coverage-gated branch addition; TDD per fix, no cross-fix confusion; cheaper than 4 dispatches). Provisional: n=1 (2026-07-18 marketing-reposition QA-R2)
- **Exemplary NEEDS_CONTEXT discipline on structural blockers (n=1, eb4 w4d, 2026-07-17):** hit out-of-scope tsc break after dep bump → root-caused via stash→install-old→tsc-clean→pop bisection (proved bump-only causation), reverted its temp edits byte-identical, stopped exactly at scope boundary instead of migrating 4 out-of-scope files. Escape-hatch clause in brief was honored precisely; keep it in every dispatch. **n=3+ by cybond w3** (A8 teardown: NEEDS_CONTEXT ×2 with zero edits saved a live feature from silent deletion; A13: lib copy bug ISSUE-flagged not worked around).
- **opus on known-red-window and integration-heavy calendar tasks — FIRM (n=4, cybond w3: A5 opener, A6 closer, A10, A11):** confinement/closure discipline consistent; self-flagged deviations sound every time. Warm redo after being REFUTED validated: A11 re-derived ground truth experimentally, superseded both its own and the reviewer's wrong hypotheses, stated "my prior claim was wrong" plainly — no anchoring (2026-07-19).
- **opus on cross-wave squash-integration + semantic port** (rebuild branch on fresh main, 3-way merge overlap, port foreign wave's files onto new model, regenerate migrations) — w3 A14: 137 tool uses, zero redos, caught a rename-hidden deletion via --no-renames recompute, review PASS zero findings. Works when the brief pre-computes file sets + names port targets with their old-model symbols. Provisional n=1 (2026-07-21). Bonus lesson: drizzle-kit generate is TTY-interactive on create-vs-rename — two-pass generate (drop, then create) is the deterministic headless workaround.
- **haiku grade-5 on plan-complete verbatim transcription tasks — FIRM, n=10 across two waves (lib slot-W1 2026-07-21 ×7; gs-p1 2026-07-22 ×3):** T1 doc section, T2 3-component widen batch, T4/T5/T6/T8/T10 single-component slots with exact code in brief; gs-p1 T1 schemas/index/helper, T2 `'use server'` actions (threat-model clause in brief → correct auth-scoped transcription), T4 mount. Avg ~15–26 tool uses. Holds when the brief IS the code (byte-for-byte plan blocks + pre-briefed gate semantics: scoped-coverage EXIT:1 expectation, quoted EXIT lines). Adapted test to existing repo helper (mockMatchMedia) without prompting at T10; gs-p1 T4 correctly reported BLOCKED under a Bash safety-classifier outage instead of working around. Pair with orchestrator diff-verification instead of reviewer dispatch for this lane.
- **sonnet grade-5 on behavioral single-component slices within a verbatim wave (n=3, lib slot-W1, 2026-07-21):** T3 full-component rewrite with test latitude, T7 controlled/uncontrolled dual-mode state rework, T9 multi-component file + fragile merge-order fix. Zero redos; self-flagged deviations sound (T7 test mock removeItem). Routing split that held: haiku when plan contains complete code, sonnet floor when task needs any judgment (state semantics, merge order, existing-test latitude).
- **sonnet lanes proven in cybond w3 (2026-07-16→21):** pre-adjudicated hardening buckets (every design decision already made by reviews — pure execution, 5 cross-file fixes one dispatch, zero findings at review; n=1); gates-runner verification-only dispatches (full DoD suite, "report don't fix" constraint prevents scope drift; n=2 — both grade 5, second one correctly surfaced an audit FAIL rather than absorbing it, then took a warm scope-widening to fix it cleanly).

- **sonnet grade-5 on GDScript/GUT verbatim lane incl. hand-authored `.tscn` scene files — FIRM (n=2 waves):** what-is-dark phase 1: 6 dispatches (scaffold, 2 pure-logic TDD, node+tscn authoring, 7-file wiring, swallow director), zero redos, zero escalations; applied the class_name-cache lesson cross-task unprompted; first-import success on hand-authored tscn every time (2026-07-21/22). dog-eat-dog wave 1: 5 sonnet dispatches (4 verbatim batches + precedent-rich lab), zero cold redos, zero escalations, all grade 5; one NEEDS_CONTEXT was a PLAN defect (float-boundary bug) verified 2 independent ways — the escalation contract working, not a lane miss (2026-07-24/25)
- **sonnet 4-round warm chain on one file (impl + 3 user-QA fixes) — zero redos, rounds 4.5–11 min** (slot-w1 app T13, 2026-07-22): corner-model rewrite, then glide/zIndex/stale-flag fixes via SendMessage to the SAME agent. What made it work: orchestrator did root-cause recon inline before each warm dispatch (lib dist z-audit, capture-retarget analysis) so each brief carried mechanism + exact fix + test sequence — returns were byte-exact to brief, enabling orchestrator diff-verify instead of reviewer dispatch on the tiny rounds. Warm lane for QA-fix loops on a just-shipped file: FIRM (n=4 this wave + 4/4 what-is-dark).
- **sonnet warm lane extends to NEW scope — FIRM (n=4 chains: gs-p1 QA-R3b; dog-eat-dog w1 lab→QA-R2→QA-R3 2026-07-25; dog-eat-dog w2 QA-R1→R2→R3 2026-07-26/27):** w1 chain crossed file domains (dev-tooling lab → core/minigame tunables → font/emoji wiring), 56s–2min rounds, zero redos; agent self-verified brief API premises live against the engine binary unprompted (FontVariation probe). w2 chain ran 3 playtest-QA rounds (behavioral fixes + a design restructure moving ladder eval across modules) at 229k cumulative tokens, zero redos, no anchoring — round 3 caught its OWN false-green regression test during red-first verification (real-frame await drift crossed the test window; rebuilt with 100s window, proved genuine red) and reported it as a process deviation unprompted. Condition unchanged: brief fully prescriptive from orchestrator recon.
- **sonnet warm lane extends to NEW scope on the same component (n=2: gs-p1 QA-R1→QA-R3b 2026-07-22 + prior):** QA-R3b dispatched a brand-new feature slice (reflections search group: schema + server action + component + 2 test trees) to the warm QA-R1 agent — zero redos, grade 5, zero contract drift with two intervening orchestrator commits in the same file (preserved unprompted-verified); also self-verified real coverage from coverage-final.json past a reporter-table quirk. Warm reuse is viable for new scope, not just fix findings, when the brief is complete.
- **opus on multi-major SDK migrations when the brief carries a dist-verified API table — n=2 one wave (GS-P0 T3 server 7-file / T4 client 6-file, ai 4→7, 2026-07-22):** zero redos, zero escalations, zero NEEDS_CONTEXT, zero API re-derivation; T4 resolved a pre-stated cross-task wire-format gap in-scope with a dist cite exactly as the brief hypothesized. The T2 verification record (21-row export-surface table) is what buys this — pre-verified cites remove the whole assumption-risk class.
- **sonnet correctly abandons broken tooling instead of thrashing** — GS-P0 T2: @ai-sdk/codemod failed 5 ways (Windows spawnSync ENOBUFS); agent diagnosed unusable, recorded the fact, pivoted to manual-migration worklist. Fact-recording deviation handling at grade 5. n=1 (2026-07-22)
- **opus on greybox/layout tasks with design latitude surfaces contradictions instead of silently deciding** — what-is-dark T8: brief carried 4 m gap vs ≤3 m jumpable hard constraint; resolved toward the hard constraint, made the wall-run route optional, flagged both calls with geometry proof. n=2 incl. side-trough falls defense at round 1 (2026-07-22)
- **opus on lib→app component ports + consumer rewires with pre-verified dist cites — n=4 one wave, zero redos (slot-W2 A2/A3/A4/QA-R1, 2026-07-22/23):** A3 delivered the wave's highest-value NEEDS_CONTEXT (proved the FormDialog shell's `noValidate` gap via a min-attr experiment + dist grep, two costed resolutions, no workaround — warm resume ~3.7 min to green after the lib patch); A4 caught the brief's missed `ActivityScheduleSlot` import with correct type-identity reasoning AND proved 12 tsc errors pre-existing via stash+rerun instead of fixing out of scope; QA-R1 refused the brief's commit step per role boundary (correct — factory rule added).

- **sonnet reliably catches PLAN defects mid-task and flags instead of working around — FIRM (3 catches in one wave, event-series 2026-07-23/24; +3 visibility-w1 2026-07-24/25):** A4 flagged a missing ownership re-check + a structurally-broken merge pattern on freq switch (both real, both plan gaps); A6 flagged a false "inline db reads" precedent. All three honored the literal instruction while flagging — zero scope creep, warm redos landed the fixes clean. Implementer flag-quality is now a load-bearing part of the review chain, not noise. Visibility-w1 repeated the pattern ×3: V2 NEEDS_CONTEXT on unscoped $inferSelect fixture consumers, V5 grep-backed "no activities create site exists" deviation, V6 resyncToken ISSUE (implemented the checklist's literal "ONLY onWeekChange" while proving the plan text wrong — empty grid on in-view week nav); all confirmed real, warm redos clean, whole wave zero cold redos.
- **sonnet grade-5 with pre-verified-cites briefs — count keeps growing (event-series A7/A8/#7-fix, 2026-07-24):** zero API re-derivation, zero NEEDS_CONTEXT across all three; #7 fix diff byte-matched the brief enabling orchestrator diff-verify instead of reviewer dispatch.
- **opus latitude-geometry lane, phase-3b rollup: +7 dispatches, grades 5 across the board after round 1 (what-is-dark 3b, 2026-07-25/28).** T5/T6/T7 zone builds + E2/E4/E5/E6 erosion passes, zero redos on all seven, closed by a whole-branch review at 0 Critical / 1 Major (a docs sentence, not geometry). Three findings worth routing on: (1) **fresh-per-new-zone is FIRM** — brief-codified lessons from the T5 chain transferred to fresh T6/T7 agents cleanly, and each fresh agent caught a brief/plan defect the warm chain would have inherited; (2) **evidence-backed under-delivery beats list-satisfaction** — E5 returned 2 of 5 requested surfaces with per-rejection proof (wall-jump landing bands, elevator-seal penetration, desk footprints) and was right; state candidate lists as "evaluate these", never "these are the targets", since orchestrator recon can't see markers, props or landing bands; (3) **the round-1 anchoring failure was a convention error, not a capability one** — opus hand-math read a `.tscn` `Transform3D` basis COLUMN-major (Godot serializes ROW-major), inverted every conclusion, and defended it with fabricated-confidence tables. Quoting the row-major convention verbatim in every subsequent brief produced zero basis misreads across the rest of the wave, including in the reviewer's own recon children. **Adjudicate transform disputes with a one-run engine probe, never a second hand-math round.**
- **opus latitude-geometry lane FIRM (n=6: what-is-dark phase-1 T8 + phase-2 T4/T4b/T5/T5b/T5c + T6, 2026-07-21/22):** zone-greybox builds with disclosure discipline — grades 5,5,5,4,4,5; every latitude choice flagged, orchestrator brief defects caught twice via frozen-source-authority clause (T5 hallucinated marker list, T6 wrong apex figure — both resolved by the brief's own "read the frozen source" line, zero cost). Warm geometry chain through a zone's QA arc decays in cost (34→24→15 tools T5c→T5d→T5e); fresh dispatch for a NEW zone pays for itself in fresh-eyes brief-error catches. Standing brief line for latitude briefs: frozen source is authority OVER inline value lists.
- **sonnet warm micro-fix lane n=9 clean in one wave (carsickyak W1, 2026-07-24):** ONE agent via SendMessage carried 9 consecutive micro-fixes (security POST-body fix + regression test, fetchPriority LCP attrs ×2, dead-font removal, QA-R1 3-part round) — every diff byte-matched the orchestrator's recon-authored brief, gates EXIT:0 each round, orchestrator diff-verify replaced reviewer dispatch throughout. ~4–6 min round-trips. Condition holds from slot-w1: orchestrator does root-cause recon first, brief carries mechanism + exact fix + test sequence.
- **sonnet on precedent-rich page tasks = opus-grade output at ~3.5 min (carsickyak T11–T13, 2026-07-24):** the brief's "read the sibling component and match its conventions" instruction did the design work; accumulated trap-note block (5 items) + pre-verified data ≈ 3× faster dispatch with zero redos.
- **opus pre-validates plan-cited utility classes against repo token config unprompted (carsickyak T10, 2026-07-24, n=1):** caught the plan's undefined `bg-charcoal/60` token — exactly the check the plan skipped; shipped theme-safe substitute + flagged.
- **sonnet warm chains carry long feel-gate/tuning waves — FIRM (momentum-lab, 2026-07-23/24):** 3 warm chains (constants / player-physics / geometry) ran 15 user feel-gate rounds, ~20 warm redos, ZERO cold redos, zero escalations, near-all grade 5 across physics implementation, in-engine live-sim verification, and report-only diagnosis dispatches. Report-only diagnosis on the owning warm chain is a proven lane: R13 round 3 REFUTED the orchestrator's own premise (aliased telemetry rate) with causal evidence from the user's CSV instead of accepting the brief's conviction — diagnosis briefs should invite refutation explicitly.

- **opus GDScript latitude lane (n=2, dog-eat-dog 9b, 2026-07-24/25):** 9b.1 QA flow round (4 ruling contracts + 8 minors, ~20 files) found + invariant-tested a design conflict the brief missed (typewriter pacing vs gesture window → separate tunable), flagged 10 latitude decisions precisely; warm redo reasoned the M-A pull ceiling from statics and hardened the ctor beyond the brief (required 4th arg blocks silent softlock reintroduction). Provisional — pair with combined-review adjudication.
- **dog-eat-dog wave 3a rollup — 8 dispatches, ALL grade 5, zero cold redos (2026-07-27/29).** Sonnet ×5 on the verbatim-GDScript/GUT lane (T1–T3, T4+T5, T6+T7, T9 lab, QA-R2a, QA-R2c), opus ×3 on design-latitude work (T8 integration, QA-R1b + QA-R2b generator redesigns). Three findings:
  - **sonnet GDScript-verbatim lane is now n=3 waves FIRM** (what-is-dark phase 1, dog-eat-dog w1, dog-eat-dog w3a) — zero cold redos across all three. Batching tasks that share a test file (T1–T3, T4+T5, T6+T7) dissolved the plan's own predicted red intermediates at no cost.
  - **opus measures its own deliverable rather than asserting it, and that is the lane's real value — FIRM (n=2 this wave).** QA-R1b instrumented a scripted ideal driver, derived the governing inequality, and reported that the SHIPPED max-difficulty setting sits past the weaveable band — the exact complaint that opened the round, surfaced before the user hit it. QA-R2b built a probe with the disputed pass switchable and measured its contribution in isolation across 3 densities × 2 seed ranges, converting "is this threshold too tight" into a proven structural conflict decidable in one pass instead of a tuning spiral. Both deleted their probes and verified removal via `git status`. **Brief a generator/physics redesign to MEASURE, not to assert.**
  - **Brief-defect catch rate 3/3 across implementers, and both warm redos this wave traced to PLAN defects rather than agent error** — consistent with the FIRM plan-defect-catch claim above, now also in the games domain. Orchestrator diff-verify replaced the reviewer dispatch on QA-R2c (verbatim lane, all five blocks byte-matched) — that lane is now **n=3 and cross-domain** (slot-w1 web ×2, dog-eat-dog games ×1).
  - Open observation, NOT yet a routing claim: three dispatches this wave returned complete correct work to disk without a completion report. A later harness notification showed the three had been stopped as still-registered background agents, so the mechanism is ambiguous between "silent completion" and a backgrounding artifact. Handling that worked either way: verify from disk, never re-ask. Watch next wave before writing a rule.
- **sonnet verbatim-TDD + probe lanes in GDScript, zero-redo wave (wid phase-3a, 2026-07-24/25, 4 dispatches all grade 5):** numeric-tripwire brief pattern ("span mismatch >0.15 m = STOP") converted silent wrong-pair risk into cheap self-verification on geometry-from-table work (n=1 provisional — reuse for any geometry-from-table brief); probe dispatch found/fixed 3 driver bugs inside probe scope with zero game-code touches and refused to loosen a threshold whose FAIL traced to a plan-premise error. SceneTree-coroutine reusable fact: `action_press` has inherent 2-tick visibility lag in headless drivers.

- **sonnet verbatim lane +3 grade-5 dispatches, zero cold redos (google-oauth-hardening, 2026-07-26):** crypto/scopes/sync, connection+actions+route, settings UI+card prompt; ONE warm redo whole wave (purely-additive coverage tests, 7 tool uses, ~1.5 min, byte-clean). Pre-flagged trap lines in briefs (noUncheckedIndexedAccess class, MSW fetch normalization) converted would-be redo rounds into clean deviations — trap-citation discipline pays even for plan-defect classes, not just AGENTS.md entries.
- **Opus 5 first dispatch data (oauth T3+T4 auth rewire, 2026-07-26, n=1):** grade 5, zero redos; self-ran the FULL DoD gate set unprompted with no observed over-verification cost (release-note claim (a) supported); zero scope drift, zero delegation without explicit brief lines for either. Keep omitting verify-scaffolding; scope/delegation lines unneeded at n=1 but harmless.

- **sonnet on spec-reversal patches landing on a pre-built seam — n=1, grade 5 (username-w1 T9, 2026-07-28):** a user rejected an approved spec decision at the Preview Gate; the reversal was 4 files and one expression, done in 17 tool uses / ~2.8 min with zero redos and three verbatim blocks byte-exact. What made it that cheap was the PLAN, not the agent — the original task shipped a labelled `STAMP SEAM` comment naming the exact replacement expression and a `PENDING USER DECISION` JSDoc naming the blast radius. Routing note: this sat in the sonnet lane rather than haiku's verbatim lane because the test step carried real latitude (mirror the file's existing mock chain), which is the documented split. Also a clean instance of the flag-don't-guess behavior — the plan's "two new cases plus one to re-check" was ambiguous, the agent recognised two bullets described the SAME test, merged them, and flagged the interpretation rather than shipping a duplicate. Plan-authoring lesson it exposes: state the expected touched-test COUNT when prescribing test changes in prose.

- **sonnet carried a full 9-task app wave alone, zero cold redos (friends-w1, 2026-07-26→29, 8 implementer dispatches: six grade 5, two grade 4):** schema+migration, queries, 5 `'use server'` actions, cross-user query, 3-file UI state machine, a **13-file** page+sidebar+boundaries task, a 2-surface select addition, and a **14-file** QA-driven redesign (3 created / 4 deleted / 7 modified). No opus escalation was needed at any size. **What bought the scale is reference-impl pointers, now n=2 with w3 and FIRM: a brief that says "mirror `friend-button.tsx`'s friends branch" or cites an on-branch task's shipped file lets sonnet hold a 13–14-file task without re-deriving pattern — cheaper than escalating to opus, and the per-file fidelity does not degrade.** Six of eight ran the full repo suite or scoped coverage unprompted. The two 4s: the TDD-carve-out slip under Weaknesses, and one docs-twin miss. Threat-model verbatim clause now n=6 across waves — two security-critical tasks here (5 actions, cross-user week query) came back with zero reviewer findings and predicate-binding proven in tests via `eq`/`or` call args.
- **Opus 5 orchestrator-inline note (friends-w1 close, 2026-07-29):** the wave's structural work — 9-commit rebase with 4 conflict sites, a migration renumber, dev-DB recovery, a scripted two-context browser QA pass, and a browser-axe sweep — ran inline with zero spawns and that was the right call (permission-classifier boundaries on destructive DDL and session minting make those steps orchestrator-only by construction). Note the cost side honestly: the same session showed 66% of usage above 150k context, so inline-structural is cheap in dispatch overhead and expensive in context.

## Standing brief lines
Moved into the implementer agent definition's `## Standing practice` section (2026-07-28) — the dispatcher no longer restates them. See `claude-config/agents/implementer.md`.

Evidence behind each, kept here as routing data: derived-example contradiction, contract won (event-series A8, n=1) · prettier `--write` before first `--check`, FIRM — reflow rounds in 2 unbriefed dispatches vs zero in 3 briefed (marketing-reposition T4/T6 vs T7/T9/QA-R2, 2026-07-18) · brief-listed premise checks, 3 consecutive dispatches answered in one pass with no NEEDS_CONTEXT round-trips (marketing-reposition T8/T9/QA-R2, 2026-07-18) · escape hatch n=4+ by motion-pass T7 (2026-07-21), stopped with ZERO edits on a missing-core-API premise (`ViewTransition` absent from installed react), evidenced with export enumeration + compiled-bundle grep + git history.

They were placed in the definition BODY, not the frontmatter `description` the plan named: `description` is resident in every session's agent listing, while the body loads only on dispatch — which is exactly when these apply.

## Weaknesses
- When the v8 reporter collapses fully-covered files, offers threshold-gate RC:0 assertion instead of the four quoted coverage lines — dispatches must name the acceptable alternative evidence form up front (2026-07-16 w3L L4; reviewer independent rerun closed it)
- Documented AGENTS.md traps missed even on opus when the brief doesn't cite them — briefs MUST cite trap entries (2026-07-14, w2.6 S14: opus impl + opus review both missed the mocked-lib trap; redo cost a full round)
- Shared spec files: bundles entries for other tasks' components unless brief says "ONLY your component" (2026-06-12, A1)
- **haiku attempts forbidden `git add .` fallback under tool-infra outage** — gs-p1 T4: Bash classifier outage blocked the briefed explicit-path commit; haiku tried `git add .` despite the brief's explicit prohibition (outage itself blocked it, no harm). Keep "explicit paths ONLY, never `git add .`/`-A`" as a hard clause in every verbatim brief; infra outage → agent reports BLOCKED, orchestrator finishes stage+commit inline after diff-verify. n=1 (2026-07-22)
- **sonnet treats `'use server'` constraints syntactically (async-only grep), not semantically (every export = endpoint needing auth)** — shipped an unauthenticated cross-user RPC on top of a plan that specified one (cybond w3 A3 Critical). Mitigation proven: state the threat model in the brief ("every async export is a client-callable POST") — produced correct auth-scoped code at A4 with zero redo. n=1 failure + **n=3 mitigation** (2026-07-17; 2026-07-23 event-series A4 — same verbatim clause, correct auth-scoped CRUD, and the agent itself flagged the plan's MISSING ownership check on updateSeries; 2026-07-25 visibility-w1 V6 — threat-model clause verbatim in brief for the app's only cross-user query → field-by-field busy mapper + SQL-level private exclusion, reviewer zero findings)
- **sonnet reaches for PS `Get-Content`/`Set-Content` on bulk renames** — mojibaked em-dashes + BOM'd a test file (documented workspace trap; recurred under pwsh, not just PS 5.1); self-recovered via git checkout + Edit redo, but briefs for any bulk rename/replace task must carry the Edit-tool-only clause explicitly. n=1 (2026-07-23 slot-W2 QA-R2, grade 4)
- **Unverified equivalence claims in reports ("byte-matches <commit>")** — probe-removal task hand-deleted instead of reverting and reported "byte-matches pre-probe state"; false (structural residue), caught by reviewer with one git diff. Reports' checkable claims must carry the verifying command's output, or the orchestrator/reviewer checks them. n=1 (2026-07-24 momentum-lab)
- **sonnet reads "verbatim block provided" as a TDD carve-out** — friends-w1 T9 wrote `friend-row.tsx`'s impl before its test and cited the combined-review lane (a REVIEWER-structure rule) as authorization; self-disclosed the deviation, otherwise byte-faithful across 14 files. Mitigation: every verbatim brief states that test-first is not waived by a provided block. n=1, provisional (2026-07-29, grade 4)
- **opus band-rule misses at RULE-INTERACTION points on latitude geometry — n=2 (what-is-dark phase-2 T5b C5 drop-floor, T5c C4 strip-trigger)** — self-check tables list every value yet miss the cross-rule trigger (gap reclassified near-limit → precision-strip rule fires). Mitigation: latitude briefs require a constraint CROSS-CHECK table (each changed element × each band rule), not per-element math alone. (2026-07-22)

## Model sweet spot
- Default **sonnet**
- **haiku** when the plan/brief contains the complete code byte-for-byte (verbatim transcription + tests) — provisional, n=7 zero-redo one wave (slot-W1 2026-07-21); requires pre-briefed gate semantics + orchestrator diff-verification
- **opus** when: 3+ files / shared state / integration flavor · novel pattern with nothing in repo to copy · high-stakes work (auth, payments, migrations) (seed — promoted from post-failure escalation signals)
- **opus alias = Opus 5 as of 2026-07-24; same override conditions apply.** Opus 5 dispatch briefs should now: (a) omit verify-your-work scaffolding — Opus 5 self-verifies unprompted and the old scaffolding causes over-verification; (b) add a scope-discipline line ("deliver what was asked at the scope intended") — Opus 5 can expand task scope; (c) cap delegation explicitly if the agent has Agent tool access — Opus 5 delegates to subagents more readily than 4.8, the inverse of the old under-delegation tuning. (release-note 2026-07-24, unvalidated — confirm at first reflect)
- Escalation: sonnet fail + signals → straight to opus, no retry; no signals → one retry then opus

## Spawn-worthiness
- Earns its cold start for any code task the spawner shouldn't hold inline (fable orchestrators never write implementation code themselves — 2026-07-02 user directive)

## Open questions
- Warm-redo regression rate (does the agent anchor on its mistake?) — data growing: what-is-dark phase 1 ran 4/4 warm redos PASS grade 5 with zero anchoring (T3 visuals addendum, T4 feature removal, T7 4-fix TDD round, T8 4-workstream tuning round) — warm lane holds for post-review addendums, design-change removals, AND multi-fix rounds when the redo brief carries orchestrator-traced root causes (2026-07-21/22)
- Does the Opus-5 scope-discipline / delegation-cap brief line actually change measured behavior, or is it a no-op? First Opus-5 wave should scorecard delegation count and scope drift against the pre-5 baseline. (release-note 2026-07-24, unvalidated — confirm at first reflect)
